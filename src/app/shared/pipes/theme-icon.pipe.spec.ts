import { ThemeIconPipe } from './theme-icon.pipe';

describe('ThemeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ThemeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
