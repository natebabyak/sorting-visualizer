import { ComplexityLatexPipe } from './complexity-latex.pipe';

describe('ComplexityLatexPipe', () => {
  it('create an instance', () => {
    const pipe = new ComplexityLatexPipe();
    expect(pipe).toBeTruthy();
  });
});
